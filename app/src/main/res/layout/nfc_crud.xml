<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layoutNFC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#AD1457">

    <!-- The first child in the layout is for the main Activity UI-->
    <RelativeLayout
        android:id="@+id/relativeLayoutNFC"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        tools:context=".tour.NFCTourActivity"
        android:background="@drawable/ic_b"
        android:gravity="center">

        <TextView
            android:id="@+id/nfcText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="NFC's Page"
            android:gravity="center"
            android:textSize="40dp"
            android:textColor="#F3E5F5"/>


    </RelativeLayout>

    <!-- Side navigation drawer UI -->
    <ListView
        android:id="@+id/navListNFC"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_gravity="left|start"
        android:background="@drawable/ic_pp"
        />

</android.support.v4.widget.DrawerLayout>




